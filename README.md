# Healthy_Prediction

健康預測與運動建議模型和範例程式碼。

內容包含：
- 資料處理與範例程式 `ml_healthy_detection_exercise_suggestion.py`
- Flask 範例應用 `app.py` / `app-en.py`
- Apple Health 匯出資料範例 (存放於 `apple_health_export/`)

說明
1. 建議建立虛擬環境後安裝 `requirements.txt`。
2. 若要推到 GitHub，請建立一個 private repository，然後在本地執行 `git remote add origin <repo-url>` 並 `git push -u origin main`。

License: 自行選擇
